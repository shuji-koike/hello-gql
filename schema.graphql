type Query {
  rev: String
  campaigns(limit: Int, offset: Int): [Campaign]
}

type Campaign {
  id: Int!
  uid: String!
  name: String!
  categories: [Category]
}

type Category {
  id: Int!
  name: String!
}
